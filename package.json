{
  "name": "records-api",
  "version": "1.0.0",
  "description": "API for retrieving records",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  },  
  "dependencies": {
    "ajv": "^8.8.2",
    "ajv-formats": "^2.1.1",
    "dotenv": "^10.0.0",
    "express": "^4.x",
    "mongoose": "^6.1.x",
    "nodemon": "^2.0.15"
  },
  "engines": {
    "node": "17.x"
  },
  "repository": {
    "type": "git"
  },
  "keywords": [
    "node",
    "heroku",
    "express"
  ],
  "license": "MIT",
  "devDependencies": {
    "jest": "^27.4.7",
    "supertest": "^6.1.6"
  }
}
